// * DEPENDS.
import React from "react";

// * CSS 
import "../../CSS/Home/SkillsList.css"

const SkillsList = () => {

    return(
        <div className="SkillsList">Hello i am the skills container</div>
    )
};

export default SkillsList;